apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: hex-arch-domain-layer
  description: Capa de dominio (entidades, agregados) de la arquitectura hexagonal.
  tags:
    - hexagonal-architecture
    - domain-layer
spec:
  type: service-part
  owner: platform-team
  lifecycle: production
  
  partOf:
    - hexagonal-architecture-templates